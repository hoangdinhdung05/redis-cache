spring:
  datasource:
    url: jdbc:mysql://mysql:3306/jwt_auth_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USER}
    password: ${DB_PASS}

  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    root: WARN
    com.example: INFO
