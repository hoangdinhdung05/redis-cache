# Production environment configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/redis-test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: dungdev
    password: 123456789
  data:
    redis:
      host: localhost
      port: 6379

# Jwt Configuration
jwt:
  secret: "mySecretKey12345678901234567890123456789012345678901234567890"

# Logging Configuration
logging:
  level:
    root: WARN
    com.example: INFO